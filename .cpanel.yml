---
deployment:
  tasks:
    - export NVM_DIR=$HOME/.nvm
    - source $NVM_DIR/nvm.sh
    - nvm use 20.19.0
    - npm ci
    - npm run build
    - rsync -a --delete .next/ $HOME/apps/omnichannel/.next/
    - mkdir -p $HOME/apps/omnichannel/tmp && touch $HOME/apps/omnichannel/tmp/restart.txt